From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Entrpi <entrpi@proton.me>
Date: Tue, 3 Feb 2026 15:54:45 +1100
Subject: [PATCH] Update README: 24 patches, add individual patch list, OPi6+
 support

---
 README.md | 31 +++++++++++++++++++++++--------
 1 file changed, 23 insertions(+), 8 deletions(-)

diff --git a/README.md b/README.md
index 982ad93c87a71..5ee76dce5ef58 100644
--- a/README.md
+++ b/README.md
@@ -8,18 +8,20 @@ This is the development repo where patches are written, tested, and rebased. Exp
 
 | Branch | Base | Patches | Purpose |
 |--------|------|---------|---------|
-| `main` | Latest 6.18.x stable tag | 13 | LTS — production kernel |
-| `rc` | Latest 6.19-rcN tag | 12 | RC — pre-release testing |
+| `main` | Latest 6.18.x stable tag | 24 | LTS — production kernel |
+| `rc` | Latest 6.19-rcN tag | 24 | RC — pre-release testing |
 | `latest` | Latest 6.19.x stable tag | — | Latest stable (when 6.19 releases) |
 | `next` | Linus's master | — | Bleeding edge development |
 | `original-patches` | v6.18.7 | 78 | Archived — pre-consolidation history |
 | `master` | — | 0 | Mirror of Linus upstream |
 
-Each track branch contains the same Sky1 driver set rebased onto its respective upstream base. The `main` branch has one extra patch (dev scripts) not present in the other tracks.
+Each track branch carries the same Sky1 patch set rebased onto its respective upstream base.
 
 ## Patch structure
 
-Patches are consolidated by subsystem. Each commit covers one functional area:
+The first 13 patches are subsystem-grouped (consolidated from 78 original bringup commits). Subsequent patches are individual fixes and features.
+
+**Subsystem patches (1–13):**
 
 1. **Device trees** — SoC base + board DTS (O6, O6N)
 2. **PCIe** — Cadence host controller, ATU, MSI, hotplug, I/O windows
@@ -33,7 +35,21 @@ Patches are consolidated by subsystem. Each commit covers one functional area:
 10. **VPU** — Linlon MVE video codec (H.264, HEVC, AV1, VP9)
 11. **IRQ / IOMMU / perf** — PDC, SMMU boot-active streams, ARM SPE
 12. **Misc platform** — Thermal, PWM, watchdog, DDR LP, bus DVFS, cpufreq
-13. **Dev scripts** — build-install, update-dev-boot, kernel-track-status (LTS only)
+13. **Dev scripts** — build-install, update-dev-boot, kernel-track-status
+
+**Individual patches (14+):**
+
+14. RTL8125/8126 shutdown panic fix
+15. Panthor SCMI perf domain DVFS
+16. GPU OPP table
+17. Panthor SCMI DVFS virtual device fix
+18. WiFi RFKill GPIO fix (O6, O6N)
+19. GPIO LEDs and pinctrl fixes (O6, O6N)
+20. Orange Pi 6 Plus device tree
+21. DPTX display driver warning fixes
+22. DRM bridge chain for OPi6+ DP-to-HDMI
+23. Panthor DVFS SCMI rate limiting
+24. OPi6+ board-specific compatible string
 
 ## Remotes
 
@@ -57,9 +73,8 @@ git push --force sky1 HEAD:main
 ### Exporting patches to linux-sky1
 
 ```bash
-rm ~/sky1-linux-distro/linux-sky1/patches/*.patch
-git format-patch -o ~/sky1-linux-distro/linux-sky1/patches \
-    --no-signature --zero-commit -M -N v6.18.Y..HEAD
+# Auto-detects output dir from branch (main → patches/, rc → patches-rc/, etc.)
+~/sky1-linux-distro/sky1-linux-build/scripts/export-patches.sh
 ```
 
 ### Building for local testing
